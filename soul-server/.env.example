# Soulstream Configuration

# === 필수 ===
WORKSPACE_DIR=/path/to/workspace

# === 인증 ===
# AUTH_BEARER_TOKEN=  # API Bearer 토큰. 미설정 시 개발 모드에서 우회

# === Claude CLI ===
# CLAUDE_CLI_DIR=  # claude가 PATH에 없는 경우 설정 (e.g., C:\Users\LG\.local\bin)

# === 서버 ===
# HOST=0.0.0.0
# PORT=3105
# ENVIRONMENT=development

# === 디렉토리 ===
# DATA_DIR=              # 태스크/이벤트 저장. 미설정 시 {CWD}/.local/data
# INCOMING_FILE_DIR=     # 첨부 파일 임시 저장. 미설정 시 {WORKSPACE_DIR}/.local/incoming

# === Runner Pool ===
# RUNNER_POOL_MAX_SIZE=5
# RUNNER_POOL_IDLE_TTL=300.0
# RUNNER_POOL_PRE_WARM=2
# RUNNER_POOL_MIN_GENERIC=1
# RUNNER_POOL_MAINTENANCE_INTERVAL=60.0

# === 리소스 ===
# MAX_CONCURRENT_SESSIONS=3
# SESSION_TIMEOUT_SECONDS=1800

# === 로깅 ===
# LOG_LEVEL=INFO
# LOG_FORMAT=json
