# 재시작 복구

R3 코드 대조 커밋 SHA: `e15bfa04bbb304f4138930a8e022f4d70ccffd4e`

> 범위 주석: accepted input producer는 boot 1회·node-ready·orch maintenance이며, 5초 maintenance lane은 stale attempt와 notification 투영만 회수한다.

> 배포 경계: 기준선의 `085a`·`085b` 다음에 비파괴 rename `086`을 적용한다. 이후 eiaserinnys(orch+host) → wsl → linegames 순으로 재시작하며, 배포 경로에는 파괴 분류나 백업 게이트가 없다.

| 단계 | 파일:심볼(라인) | 이 단계가 소유한 사실 | 거부/분기 조건 |
| --- | --- | --- | --- |
| 1. Claude 복구 객체 조립 | `soul-server-ts/src/runtime/claude_runtime_composition.ts:composeClaudeRuntime` (L51–114) | transcript queued recovery와 SDK background lifecycle을 조립하되 두 판단을 묶지 않는다. queued delivery만 `ClaudeRuntimeStartupRecovery`가 맡고 background terminal 판단은 runner recovery에 전달한다. | runtime v2가 꺼져 있으면 두 객체 모두 만들지 않는다. |
| 2. background terminal owner | `soul-server-ts/src/runtime/worker_composition.ts:composeWorkerRuntime` (L293–308), `runner/runner_recovery_coordinator.ts:handle` (L310–447), `task/task_executor.ts:retainRegisteredClaudeBackgroundRunner` (L534–564) | worker composition의 active-row 전량 `worker_restart` 종결을 삭제했다. 초기 runner scan이 살아 있는 terminal child를 host-only adopt해 SDK live background set을 묻고, set이 비지 않았으면 동일 process owner를 유지한다. | execution command terminal만으로 process를 닫지 않는다. kernel lock이 held인데 live set 조회가 실패하면 보수적으로 runner를 유지한다. |
| 3. listener·upstream 선행 | `soul-server-ts/src/runtime/worker_startup.ts:startWorkerRuntime` (L34–46), `upstream/adapter.ts:UpstreamAdapter.connectAndServe` (L342–354), `release/release_activation_state.ts:ReleaseActivationState.acceptReceipt` (L64–95) | listener를 먼저 열고 upstream adapter를 시작한다. 중앙 `node_register_ack`의 activation receipt가 검증되면 등록 readiness를 단 한 번 연다. | adapter 연결과 runner scan은 기존 순서대로 즉시 시작하며, startup 반환은 ACK를 기다리지 않는다. upstream 실패는 별도 handler로 격리한다. |
| 4. runner 초기 수렴 | `soul-server-ts/src/runtime/worker_startup.ts:startWorkerRuntime` (L48–59), `runner/runner_recovery_coordinator.ts:RunnerRecoveryCoordinator.start` (L139–149) | runner registration 초기 scan에서 adopt·live-set owner 복원·dead-row terminalize를 먼저 수렴시키고, queued transcript pass 직전 중앙 등록 readiness와 합류한다. | runner process mode가 아니면 scan은 즉시 수렴하지만 등록 ACK 경계는 유지한다. process가 살아 있으면 background 행을 종결하지 않는다. |
| 5. queued delivery boot cursor | `soul-server-ts/src/runtime/worker_composition.ts:composeWorkerRuntime` (L488–497), `worker_startup.ts:startWorkerRuntime` (L55–65), `claude_runtime_startup_recovery.ts:afterRunnerRecovery` (L38–47) | runner 수렴과 upstream 등록 ACK가 모두 끝난 뒤 queued delivery snapshot을 boot당 정확히 한 번 실행한다. 따라서 연결 대기 시간은 15초 recovery step deadline을 소비하지 않는다. | stopped면 생략; ACK 전에는 pass를 시작하지 않으며 cached promise가 같은 process의 두 번째 snapshot을 차단한다. |
| 6. boot queued claim | `soul-server-ts/src/task/queued_delivery_transcript_recovery.ts:recoverAfterNodeRestart` (L70–88), `orch-server-ts/src/control_plane/repositories/session_delivery_recovery_repository.ts:claimQueuedAfterNodeRestart` (L176–188) | 이 node의 `queued` delivery만 60초 attempt TTL로 한 번 claim한다. `includeDelivered=false`가 boot owner의 고정 계약이다. | delivered receipt가 있는 행과 aggregate consumed/dead-letter, 미도래 행은 claim하지 않아 attempt count·dead letter·user message를 새로 만들지 않는다. |
| 7. transcript 대조 | `soul-server-ts/src/task/queued_delivery_transcript_recovery.ts:reconcile` (L90–186), `task_intervention_route.ts:TaskInterventionRoute.addIntervention`, `task_auto_resume_transition.ts:AutoResumeTransition.resume`, `compensateRunningTransition`, `engine/claude_session_runtime.ts:enqueueInput` (L161–177) | boot에서 claim한 queued 행만 Claude transcript와 대조한다. completed receipt는 consumed로 확정하고, `absent` 재발행 직전 ledger를 다시 읽어 live turn이 그 사이 consumed+receipt를 남겼으면 재발행 없이 종결한다. 실제 receipt·outbox가 모두 없는 queued 행만 같은 identity로 ordinary durable intervention을 정확히 한 번 재전달한다. | delivered 행은 이 단계에 들어오지 않는다. queued 행도 consumed receipt가 없고 transcript가 `absent`일 때만 원 content를 재전달한다. unknown/input_pending은 기존 #775 보수 정책대로 재검사하며 attempt 예산을 쓰지 않는다. |
| 8. transcript receipt 투영 | `orch-server-ts/src/control_plane/repositories/session_delivery_recovery_repository.ts:markDeliveredFromTranscript` (L205–244) | assistant message UUID를 `transcript:*` receipt로 기록해 `delivered/delivered`를 확정한다. | 정확한 attempt token을 가진 `claimed` row만 전이한다. |
| 9. node-ready 트리거 | `orch-server-ts/src/production.ts:createProductionRuntime` (L225–238) | 새 node connection의 session cache seed 완료가 즉시 pending immediate replay를 한 번 호출한다. | node-ready 이벤트가 없으면 실행되지 않으며 timer가 아니다. |
| 10. node-ready claim·전송 | `orch-server-ts/src/production.ts:replayPendingImmediateDeliveriesForNode` (L566–633) | `node-ready:{node}:{connection}` attempt token으로 해당 node의 pending immediate delivery를 enqueue 순서대로 claim·route·전송한다. | identity 불완전·route/command 실패면 자기 attempt token이 남은 row만 즉시 pending retry로 되돌린다. |
| 11. orch maintenance 트리거 | `orch-server-ts/src/runtime/orchestrator_maintenance_service.ts:OrchestratorMaintenanceService.start` (L56–62), `runMaintenanceOnce` (L96–105) | 기동 즉시와 60초마다 연결된 node의 pending replay를 보조 트리거로 호출한다. | 이전 delivery recovery가 진행 중이면 중첩 실행하지 않는다. |
| 12. orch maintenance replay | `orch-server-ts/src/production.ts:createProductionRuntime` (L499–536) | 연결 목록을 순회하며 `maintenance:{node}:{connection}` 소유자로 같은 replay 정본을 재사용한다. | node별 실패는 다른 node replay를 중단하지 않는다. |
| 13. stale attempt maintenance | `soul-server-ts/src/task/completion_delivery_recovery_worker.ts:CompletionDeliveryRecoveryWorker` (L30–66), `completion_delivery_coordinator.ts:recoverPending` (L117–123) | 기동 즉시와 5초마다 expired admission attempt와 notification outbox 투영을 회수한다. | accepted input을 claim·dispatch·auto-resume하지 않는다; step별 90초 deadline으로 격리한다. |
| 14. runner registration scan | `soul-server-ts/src/runner/runner_recovery_coordinator.ts:RunnerRecoveryCoordinator.performScan` (L146–298) | node-local registration과 손상 registration을 읽고 session별 recovery disposition을 직렬화한다. 중앙 owner-null inventory API·client·TaskManager bridge는 삭제되어 node-local scan만 남는다. | active·adoption-pending·retired terminal은 재진입하지 않는다. |
| 15. runner 상태 분류 | `soul-server-ts/src/runner/runner_process_registry.ts:scanRunnerRegistrations` (L92–130), `classifyRunnerRegistration` (L133–168), `runner_registration_reader.ts:readRunnerRegistrationSummary` (L111–138) | registration·lifecycle과 kernel-lock 기반 생사 값을 `wait/adopt/replay/reap/closed/retired` 중 하나로 분류한다. | progress 시각만으로 process death를 추론하지 않고 lock unavailable은 보수적으로 live 취급하며, exact owner 검증은 adopt 경계가 소유한다. |
| 16. runner 복구 실행 | `soul-server-ts/src/runner/runner_recovery_coordinator.ts:RunnerRecoveryCoordinator.handle`, `recoverRegistered`, `terminalizeDeadClaudeBackgroundTasks`, `retireAttachedTerminalAdmission`, `task/task_executor.ts:retainRegisteredClaudeBackgroundRunner`, `task/claude_background_task_lifecycle.ts:terminalizeDeadRunner`, `runner/runner_process_dispatcher.ts:startRecovery`, `attachConnection`, `reconcileActiveLifecycleTerminal` | adopt·terminal replay·dead reap·closed tail drain을 disposition별 단일 경로로 실행한다. `replay_terminal`은 SDK live set이 process owner이면 `blockedBy: background`로 보존한다. `replay_terminal_dead`와 재검증된 `reap_dead`만 session-scoped active rows를 `killed/worker_restart`로 종결하고 follow-up을 한 번 생성한다. | 다른 registration·command의 successor는 건드리지 않는다. process death가 증명되지 않으면 background row terminalize·runner detach·terminate·retire를 모두 금지한다. 새 timer·scan·replacement 없이 기존 반복 scan과 SDK notification만 사용한다. |
| 17. 러너 host 공백 유지 | `soul-server-ts/src/runner/runner_child_runtime.ts:drainExecutionWithBuffer` (L297–345), `forwardRunnerFrame` (L347–409), `runner_host_request_client.ts:RunnerHostUnavailableError` (L24–115) | host IPC가 잠시 없어 request·snapshot 전달 예산을 넘어도 러너는 자기 턴을 interrupt·terminal 처리하지 않는다. 실행을 유지하고, durable engine event는 SQLite outbox에 적재해 재부착 후 replay한다. IPC journal replay 범위는 현재 registration의 미ACK 프레임뿐이며, 다른 registration의 미ACK 행은 replay 없이 `host_acked`로 종결한다. | `RunnerHostUnavailableError`만 실행 유지 분기로 소유한다. host가 응답한 서비스 오류·저장 실패·엔진 실패는 기존 terminal 경로를 타며, 명시적 interrupt의 소유자는 host다. schedule control처럼 host 응답이 필요한 request는 기존 예산 내 전달에 실패하면 해당 요청만 실패로 응답하고 엔진 턴은 계속한다. |
| 18. runner 반복 scan | `soul-server-ts/src/runtime/runner_process_composition.ts:composeRunnerRecoveryCoordinator` (L81–129), `runner_recovery_coordinator.ts:start` (L139–149) | 초기 scan 후 env의 `SOUL_RUNNER_REAPER_INTERVAL_MS` cadence로 registration과 kernel lock 상태 변화를 계속 회수한다. | runner factory/state dir가 없으면 coordinator를 만들지 않으며 scan in-flight는 합류한다. |
| 19. upstream inventory 재보고 | `soul-server-ts/src/upstream/initial_runner_state_sync.ts:sendInitialRunnerState` (L11–58) | 현재 connection에 runner inventory를 최대 5회 보고하여 orch가 node-ready session-node mapping을 다시 구성하게 한다. | connection 교체·WebSocket close면 즉시 중단; 5회 실패 뒤 현재 connection에서 종료한다. |

이 장을 갱신해야 하는 변경 부류: worker startup 순서·queued transcript recovery·node-ready/maintenance replay·delivery attempt maintenance·runner scan/classification/adoption·host gap frame forwarding·initial inventory 변경.
