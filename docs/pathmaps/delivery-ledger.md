# 전달 원장 상태 전이

최종 대조 커밋 SHA: `bdedb646fb505567bdabfd37d71821bd5dbdec3f`

> 범위 주석: `state`는 현재 시도, `aggregate_state`는 논리 delivery의 종합 결과이며 둘을 함께 쓰는 모든 production writer를 열거한다.

| 단계 | 파일:심볼(라인) | 이 단계가 소유한 사실 | 거부/분기 조건 |
| --- | --- | --- | --- |
| 1. 상태 어휘 | `packages/db-schema/sql/schema.sql:session_deliveries` (L386–437), `session_deliveries_aggregate_state_check` (L3677–3681) | `state={pending,claimed,dispatching,queued,delivered,consumed,superseded,uncertain}`, `aggregate_state={pending,delivered,consumed,dead_letter}`가 유일한 저장 어휘다. | CHECK 밖의 값은 DB가 거부한다. |
| 2. 일반 등록 | `orch-server-ts/src/control_plane/repositories/session_delivery_relation_repository.ts:registerSessionDelivery` (L24–133) | 일반 producer가 새 row를 `pending/pending`으로 만든다; exact relation tombstone이 이미 있으면 즉시 `consumed/consumed`로 만든다. | delivery/relation identity 충돌은 3번으로 간다; 동일 등록은 기존 row를 반환한다. |
| 3. 등록 identity 충돌 | `orch-server-ts/src/control_plane/repositories/session_delivery_relation_repository.ts:registerSessionDelivery` (L102–132), `session_delivery_runtime_followup_repository.ts:resolveRegistrationConflict` (L189–223) | 일반·runtime follow-up 등록 충돌을 `*/ * → uncertain/dead_letter`로 종결하고 rejected attempt를 남긴다. | 기존 `consumed`·`superseded` row는 덮지 않는다. |
| 4. runtime follow-up 최신성 등록 | `orch-server-ts/src/control_plane/repositories/session_delivery_runtime_followup_repository.ts:registerRuntimeFollowupDelivery` (L19–164) | follow-up key advisory lock 안에서 최신 후보는 `pending/pending`, 이미 더 새 후보가 있으면 `superseded/consumed`로 생성·전이한다. | 동일 identity는 멱등 반환; attempt·created_at·enqueue_sequence 순으로 최신을 고른다. |
| 5. runtime follow-up 형제 대체 | `orch-server-ts/src/control_plane/repositories/session_delivery_runtime_followup_repository.ts:supersedePendingRuntimeFollowupSiblings` (L166–187) | 같은 target+followup key의 이전 `pending/pending → superseded/consumed`를 단일 최신 후보로 정리한다. | retained delivery와 pending이 아닌 형제는 건드리지 않는다. |
| 6. 최초 claim | `orch-server-ts/src/control_plane/repositories/session_delivery_repository.ts:claim` (L89–106), `claimForTarget` (L108–131), `soul-server-ts/src/task/task_delivery_ledger_gate.ts:admit` (L56–142) | 활성 route/completion owner는 `claimForTarget`으로 `pending\|queued / pending → claimed/pending` 대상과 15초 lease를 확보한다. generic `claim` API는 기준 SHA의 production source에 호출자가 없다. | 대상 session 부재·state CAS 실패·aggregate terminal은 claim하지 않는다. |
| 7. node-ready claim | `orch-server-ts/src/control_plane/repositories/session_delivery_recovery_repository.ts:claimPendingImmediateIntentsForNode` (L41–79) | orch node-ready/maintenance owner가 해당 node의 due `pending/pending → claimed/pending`을 enqueue 순서대로 claim한다. | 다른 node·미도래·이미 claimed row는 제외한다. |
| 7A. completion recovery claim API | `orch-server-ts/src/control_plane/repositories/session_delivery_recovery_repository.ts:claimRecoverableCompletionDeliveries` (L81–174) | terminal-relation settlement 뒤 최신 runtime follow-up만 남기고 due `pending/pending → claimed/pending`을 target에 결속한다. | target이 없으면 retry/dead-letter policy로 되돌리며 기준 SHA의 production source에는 이 claim API 호출자가 없다. |
| 8. dispatch 시작 | `orch-server-ts/src/control_plane/repositories/session_delivery_repository.ts:beginDispatch` (L133–163), `soul-server-ts/src/task/task_delivery_ledger_gate.ts:beginDispatch` (L145–164) | routing 직전 `claimed/pending → dispatching/pending`을 확정한다. | lease owner/만료가 맞지 않거나 completion producer terminal revision이 canonical terminal과 다르면 거부한다. |
| 9. 일반 durable queue | `orch-server-ts/src/control_plane/repositories/session_delivery_repository.ts:markQueued` (L287–311), `soul-server-ts/src/task/task_delivery_ledger_gate.ts:recordResult` (L203–254), `task_executor.ts:driveQueuedAfterTerminal` (L457–482), `task_auto_resume_transition.ts:resumeQueuedAfterTerminal` (L54–63) | human/durable-next-turn의 내구 접수를 `dispatching/pending → queued/pending`으로 확정한다. running 턴의 terminal 꼬리에 남은 in-memory queue는 execution slot 해제 뒤 기존 auto-resume owner를 즉시 구동하지만 ledger row는 그대로 `queued/pending`에 두며, 성공한 다음 턴이 18단계에서 exact delivery를 소비한다. | lease owner CAS가 맞지 않으면 기존 동일 queued row만 멱등 성공으로 인정한다. terminal이 아니거나 새 실행이 이미 생겼거나 queue가 비었으면 post-terminal driver는 원장과 task를 건드리지 않는다. |
| 10. notification queue+outbox | `orch-server-ts/src/control_plane/repositories/session_delivery_notification_repository.ts:stageWithQueuedDelivery` (L28–125) | completion/runtime notification을 `dispatching/pending → queued/pending`으로 전이하면서 outbox `claimed/publishing`을 같은 transaction에 만든다. 같은 delivery의 기존 outbox가 exact `delivery_id+completion_id+relation_key+delivery_intent`이고 unleased pending이거나 같은 owner·만료 lease의 publishing이면 새 시도가 멱등 재점유한다. | delivery lease 불일치, live 경쟁 owner, identity 불일치는 전체 transaction을 거부한다. |
| 11. pending·leased retry | `orch-server-ts/src/control_plane/repositories/session_delivery_repository.ts:deferPending` (L177–203), `retryLeasedDelivery` (L205–234), `session_delivery_retry_policy.ts:deliveryRetryOrDeadLetterSet` (L58–95) | 실패 owner가 `pending\|claimed\|dispatching\|queued → pending/pending`으로 backoff하거나 예산 소진 시 `uncertain/dead_letter`로 전이한다. | claimed 계열은 정확한 lease owner만; 16회 또는 24시간 ceiling이면 retry하지 않는다. |
| 12. expired admission lease | `orch-server-ts/src/control_plane/repositories/session_delivery_repository.ts:releaseExpiredDeliveryLeases` (L257–284) | maintenance가 만료된 `claimed\|dispatching → pending/pending` 또는 `uncertain/dead_letter`로 회수한다. | 만료 전 lease·queued row는 건드리지 않는다. |
| 13. queued transcript claim | `orch-server-ts/src/control_plane/repositories/session_delivery_recovery_repository.ts:claimQueued` (L285–363) | boot transcript owner가 `queued/pending → claimed/pending`; 허용된 재대조는 `delivered/delivered → claimed/delivered`로 lease만 옮긴다. | terminal aggregate·미도래·node/restart scan 조건 밖 row는 제외한다. |
| 14. transcript receipt | `orch-server-ts/src/control_plane/repositories/session_delivery_recovery_repository.ts:markDeliveredFromTranscript` (L205–244) | exact assistant UUID 증거로 `claimed/pending\|delivered → delivered/delivered`와 target receipt를 확정한다. | 정확한 lease owner의 claimed row만 전이한다. |
| 15. transcript 재검사 defer | `orch-server-ts/src/control_plane/repositories/session_delivery_recovery_repository.ts:deferQueuedTranscriptCheck` (L247–283) | 불명확한 transcript를 `claimed/* → queued/pending`으로 되돌리되 probe는 attempt 수를 쓰지 않는다. | 24시간 age ceiling에 닿으면 `uncertain/dead_letter`; 이때만 attempt ledger를 남긴다. |
| 16. 직접 delivered API | `orch-server-ts/src/control_plane/repositories/session_delivery_repository.ts:markDelivered` (L313–333) | `dispatching\|claimed\|queued / pending → delivered/delivered`와 caller turn receipt를 기록하는 공용 writer다. | 기준 SHA의 production source에 직접 호출자는 없으며 aggregate가 pending이 아니면 전이하지 않는다. |
| 17. notification receipt | `orch-server-ts/src/control_plane/repositories/session_delivery_notification_repository.ts:markPublished` (L131–185), `soul-server-ts/src/task/notification_receipt_projection.ts:projectNotificationReceipt` (L14–40) | outbox publish receipt를 가진 row는 aggregate pending일 때 underlying delivery를 `*/pending → delivered/delivered`로 투영한다. | outbox lease·receipt가 없거나 aggregate와 기존 receipt가 충돌하면 transaction을 거부한다. |
| 18. exact delivery 소비 | `orch-server-ts/src/control_plane/repositories/session_delivery_consumption_transition.ts:markSessionDeliveryConsumed` (L13–37), `soul-server-ts/src/task/task_delivery_ledger_gate.ts:recordConsumed` (L397–428) | 성공 턴이 `pending\|claimed\|dispatching\|queued\|delivered / pending\|delivered → consumed/consumed`와 immutable receipt를 기록한다. | 이미 terminal aggregate이거나 delivery id가 다르면 전이하지 않는다. |
| 19. relation 소비 | `orch-server-ts/src/control_plane/repositories/session_delivery_relation_repository.ts:recordRelationConsumedInTransaction` (L237–293), `orch-server-ts/src/control_plane/repositories/session_delivery_repository.ts:markConsumedByRelation` (L347–369) | child completion의 exact relation tombstone을 쓰고 일치 delivery를 `pending\|claimed\|delivered → consumed/consumed`로 만든다. | completion/caller identity 충돌은 transaction 오류; 다른 relation은 건드리지 않는다. |
| 20. terminal target relation 복구 | `orch-server-ts/src/control_plane/repositories/session_delivery_terminal_target_transition.ts:settleTerminalTargetCompletionDeliveries` (L11–48) | maintenance/claim 전에 exact relation receipt가 있는 completion row를 모든 nonterminal 시도 상태에서 `consumed/consumed`로 수렴시킨다. | target lifecycle만으로는 소비하지 않고 exact relation receipt를 요구한다. |
| 21. 명시적 pending 대체 | `orch-server-ts/src/control_plane/repositories/session_delivery_repository.ts:markPendingSuperseded` (L236–255), `soul-server-ts/src/task/task_delivery_ledger_gate.ts:recordPendingSuperseded` (L430–441) | producer가 낡은 `pending/pending → superseded/consumed`를 terminal revision 이유와 함께 닫는다. | pending이 아닌 row는 대체하지 않는다. |
| 22. terminal resume 대체 | `packages/db-schema/sql/schema.sql:session_apply_running_transition` (L1662–1723), `session_acquire_execution_ownership` (L5427–5516) | child session의 같은 terminal revision이 resume되면 미발행 completion `pending\|claimed\|dispatching\|queued → superseded/consumed`를 running transition 안에서 닫는다. 뒤이은 호환 함수는 owner를 선출하지 않고 exact runtime registration identity를 기록한다. legacy generation 증가는 Wave 3 스키마 정리 전 내부 호환값일 뿐 event stale fence로 송출·비교하지 않는다. | terminal revision CAS가 실제 적용된 경우만; 다른 source/producer/revision은 보존한다. registration identity 기록 거부는 새 실행을 시작하지 않는다. |
| 23. 명시적 uncertain | `orch-server-ts/src/control_plane/repositories/session_delivery_repository.ts:markUncertain` (L371–419), `soul-server-ts/src/task/task_delivery_ledger_gate.ts:recordResult` (L260–287) | 비수용 결과·stale self completion·재시도 소진을 `nonterminal → uncertain/dead_letter`로 종결한다. | consumed/dead-letter aggregate·consumed/superseded state는 덮지 않으며 lease가 주어지면 owner까지 맞아야 한다. |
| 24. notification retry·dead-letter | `orch-server-ts/src/control_plane/repositories/session_delivery_notification_repository.ts:retry` (L187–243), `deadLetter` (L245–282), `releaseExpiredLeases` (L328–400) | outbox 실패는 underlying `aggregate_state`만 pending 또는 dead_letter로 바꾸며 현재 delivery `state`는 queued 등 기존 값을 보존한다. | consumed/dead-letter aggregate는 덮지 않고 outbox lease owner·16회·24시간 ceiling을 따른다. |
| 25. notification 수동 재queue | `orch-server-ts/src/control_plane/repositories/session_delivery_notification_repository.ts:requeueDeadLetter` (L295–326) | 운영 재queue가 outbox dead-letter를 pending으로, underlying `*/dead_letter → */pending`으로 되돌린다. | discarded outbox나 dead-letter가 아닌 aggregate는 바꾸지 않는다. |
| 26. schema bootstrap 정규화 | `packages/db-schema/sql/schema.sql:session_deliveries aggregate backfill` (L3707–3812), `notification outbox reconciliation` (L3814–3864) | 기존 설치의 legacy state·receipt·outbox를 현재 aggregate 어휘로 1회 정규화한다. | schema 적용 경계 전용이며 request/recovery runtime writer가 아니다. |

이 장을 갱신해야 하는 변경 부류: `session_deliveries` 스키마·등록/claim/dispatch/queue/retry/receipt/consume/supersede/dead-letter writer·notification outbox·terminal resume DB 함수 변경.
