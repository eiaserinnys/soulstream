# 복구·전달·러너 타이머 목록

최종 대조 커밋 SHA: `24da8a5b9027bb9265a3d50088d4474645355a98`

> 상태 배지: **Wave 1 runtime ownership machine 제거 반영**

> 범위 주석: 이 5장이 다루는 message·delivery·restart·runner 경로의 active cadence와 deadline만 열거하며, timer가 아닌 event trigger도 중복 producer 판정을 위해 포함한다.

| 단계 | 파일:심볼(라인) | 이 단계가 소유한 사실 | 거부/분기 조건 |
| --- | --- | --- | --- |
| 1. queued transcript boot pass | `soul-server-ts/src/runtime/claude_runtime_startup_recovery.ts:afterRunnerRecovery` (L38–47), `worker_startup.ts:startWorkerRuntime` (L48–65), `release/release_activation_state.ts:ReleaseActivationState.waitUntilReady` (L64–95) | **owner:** worker startup; **cadence:** runner 초기 scan과 중앙 upstream 등록 ACK 뒤 boot당 1회; **목적:** queued transcript reconciliation; **판정:** R27에서 5초 반복 삭제, R25c에서 연결 대기를 pass deadline 밖으로 이동, one-shot 유지. | ACK 전에는 pass를 시작하지 않으며 cached promise·stopped가 같은 boot 재실행을 차단한다. |
| 2. startup recovery deadline | `soul-server-ts/src/runtime/claude_runtime_startup_recovery.ts:STARTUP_RECOVERY_STEP_TIMEOUT_MS` (L7–12), `recoverQueuedDeliveries` (L63–83) | **owner:** startup recovery; **cadence:** 각 boot step 최대 15초; **목적:** startup hang 격리; **판정:** 유지. | deadline 실패를 periodic retry로 승격하지 않고 해당 boot에서 종료한다. |
| 3. transcript attempt TTL·read deadline | `soul-server-ts/src/task/queued_delivery_transcript_recovery.ts:TRANSCRIPT_ATTEMPT_TTL_MS/TRANSCRIPT_READ_TIMEOUT_MS` (L47–65), `reconcile` (L88–101) | **owner:** queued transcript pass; **cadence:** claim attempt TTL 60초·row read 10초; **목적:** batch가 자기 attempt TTL을 넘지 않게 함; **판정:** 유지. | 남은 attempt TTL이 read 1회보다 짧으면 새 row 처리를 시작하지 않는다. |
| 4. delivery maintenance lane | `soul-server-ts/src/task/completion_delivery_recovery_worker.ts:CompletionDeliveryRecoveryWorker` (L30–66), `runtime/periodic_maintenance_loop.ts:start` (L144–163) | **owner:** soul worker `session-deliveries`; **cadence:** 기동 즉시+5초; **목적:** stale delivery attempt·notification outbox 회수; **판정:** 유지. | active tick이 deadline 안이면 중첩 tick을 억제하고 accepted input을 새로 dispatch하지 않는다. |
| 5. delivery maintenance step deadline | `soul-server-ts/src/task/completion_delivery_recovery_worker.ts:RECOVERY_STEP_TIMEOUT_MS` (L5–13), `runtime/periodic_maintenance_loop.ts:PeriodicMaintenanceLoop` (L119–139) | **owner:** maintenance lane; **cadence:** step당 90초, tick deadline은 step deadline 합의 2배; **목적:** hung step 격리; **판정:** 유지. | outstanding step은 재발행하지 않고 sibling step은 계속 실행한다. |
| 6. delivery maintenance liveness | `soul-server-ts/src/runtime/periodic_maintenance_loop.ts:DEFAULT_LIVENESS_INTERVAL_MS` (L29–31), `start` (L150–162) | **owner:** maintenance loop; **cadence:** 60초; **목적:** tick/timeout/skip 카운터 관측; **판정:** 유지. | `livenessIntervalMs=0`인 별도 구성만 끌 수 있다. |
| 7. completion dispatch attempt·deadline | `soul-server-ts/src/task/completion_delivery_coordinator.ts:CompletionDeliveryCoordinator.constructor` (L66–93), `dispatchClaimed` (L152–235) | **owner:** explicit completion enqueue; **cadence:** attempt TTL 60초·dispatch deadline 15초; **목적:** 한 dispatch가 자기 attempt TTL을 넘지 않게 함; **판정:** R33 보완 필요. | timeout은 `retryDeliveryAttempt`만 예약한다. runner IPC·Claude transport·CLI는 재수립하지 않으므로 같은 running-but-deaf 상태에서 반복 실패할 수 있다. |
| 8. notification outbox recovery | `soul-server-ts/src/task/session_delivery_notification_recovery.ts:recover` (L28–48), `orch-server-ts/src/control_plane/repositories/session_delivery_notification_repository.ts:DEFAULT_NOTIFICATION_ATTEMPT_TTL_MS` (L12–14) | **owner:** 4번 lane의 notification step; **cadence:** 기동 즉시+5초, claim attempt TTL 15초; **목적:** queued notification 투영; **판정:** 유지. | target node의 due outbox만 claim하고 nonretryable payload는 dead-letter한다. |
| 9. node-ready replay | `orch-server-ts/src/production.ts:onNodeReady` (L225–238), `replayPendingImmediateDeliveriesForNode` (L566–633) | **owner:** orch connection event; **cadence:** node connection당 1회, timer 아님; **목적:** 연결 직후 pending immediate delivery 회수; **판정:** 유지. | 같은 connection의 별도 timer를 만들지 않으며 route 실패는 자기 attempt만 반납한다. |
| 10. orch maintenance replay | `orch-server-ts/src/runtime/orchestrator_maintenance_service.ts:ORCHESTRATOR_MAINTENANCE_INTERVAL_MS` (L8–9), `start` (L56–62), `runMaintenanceOnce` (L96–105) | **owner:** orch maintenance; **cadence:** 기동 즉시+60초; **목적:** 연결 node pending replay 보조와 cache sweep; **판정:** 유지. | 이전 delivery recovery가 진행 중이면 중첩하지 않는다. |
| 11. delivery retry ladder | `orch-server-ts/src/control_plane/repositories/session_delivery_retry_policy.ts:DELIVERY_MAX_*` (L5–12), `deliveryRetryOrDeadLetterSet` (L58–95) | **owner:** DB delivery retry policy; **cadence:** 100ms 지수 증가, 최대 60초·16회·24시간; **목적:** retry pacing과 terminal ceiling 단일화; **판정:** 유지. | ceiling이면 `uncertain/dead_letter`; transcript liveness probe는 attempt를 쓰지 않고 age만 쓴다. |
| 12. runtime follow-up receipt recovery | `soul-server-ts/src/task/task_executor.ts:handleClaudeRuntimeFollowupReceipt` (L1466–1519), `task_delivery_turn_receipt.ts:hasConsumptionReceipt` (L52–56), `claude_runtime_task_followup.ts:flushedTaskKeys` (L61–62, L226–228) | **owner:** delivery receipt와 원 runtime follow-up producer; **cadence:** 턴 이벤트 기반, 별도 timer 없음; **목적:** 소비 여부를 exact delivery 하나에 기록하고, receipt 없는 같은 delivery·stable input UUID를 일반 큐와 분리된 실행 로컬 FIFO에서 한 번만 재전달; **판정:** R32에서 5초·30초 child fallback과 최대 3회 ceiling 삭제 완료. | receipt가 있으면 응답이 비어도 재주입하지 않는다. exact replay가 대기 중일 때만 high-priority 사용자 turn을 먼저 별도 실행하며, replay가 없으면 기존 high+low 단일 turn을 유지한다. 두 번째도 receipt가 없으면 원 delivery를 미소비로 남겨 startup transcript recovery가 회수한다. retry wrapper·attempt counter·fatal stall은 만들지 않으며 R33의 무해한 dispatch 재수립 전에는 R32 단독 머지 금지. |
| 13. runtime follow-up no-output watchdog | `soul-server-ts/src/engine/claude_sdk_client.ts:ClaudeSdkClient.constructor` (L121–126), `claude_runtime_followup_watchdog.ts:arm` (L38–45) | **owner:** persistent Claude session; **cadence:** runtime follow-up turn당 기본 30초; **목적:** foreground progress 없는 follow-up 탐지; **판정:** 유지. | 일반 turn deadline을 바꾸지 않으며 own input/output 관측 시 해제한다. |
| 14. event outbox retry flush | `soul-server-ts/src/upstream/event_outbox_pump.ts:DEFAULT_RETRY_FLUSH_DELAY_MS` (L35–43), `scheduleRetryFlush` (L389–403) | **owner:** node event outbox; **cadence:** retryable rejection 뒤 1초; **목적:** 같은 stream/source_seq durable batch 재전송; **판정:** 유지. | generation이 바뀌면 stale timer는 전송하지 않으며 orch가 같은 retry를 소유하지 않는다. |
| 15. event ACK deadline | `soul-server-ts/src/upstream/event_outbox_pump.ts:DEFAULT_ACKNOWLEDGEMENT_TIMEOUT_MS` (L38–43), `waitForDeferredAcknowledgement` (L275–307) | **owner:** source_seq waiter; **cadence:** waiter당 기본 30초; **목적:** caller 대기 상한; **판정:** 유지. | timeout은 waiter만 제거하고 durable outbox record와 reconnect replay를 폐기하지 않는다. |
| 16. orch event-ingress DB retry | `orch-server-ts/src/node/event_ingress_retry_policy.ts:DEFAULT_RETRY_DELAYS_MS` (L4–5), `waitForRetry` (L85–95) | **owner:** orch event ingress; **cadence:** 250·500·1000·2000ms, 총 5회; **목적:** transient DB commit 회복; **판정:** 유지. | 독립 DB probe도 실패하면 poison count를 올리지 않고 infrastructure 오류로 올린다. |
| 17. runner recovery scan | `soul-server-ts/src/config.ts:SOUL_RUNNER_REAPER_INTERVAL_MS`, `runner/runner_recovery_coordinator.ts:start` (L130–145), `performScan` (L146–298) | **owner:** node runner recovery; **cadence:** 기동 즉시+기본 15초; **목적:** local registration adopt/replay/reap/GC trigger와 중앙 row 부재·kernel-lock free residue의 terminal retirement; **판정:** 기존 cadence 재사용, owner-null inventory/backfill loop 없음. | scan in-flight는 합류하고 process mode가 아니면 timer가 없다. 중앙 hydration 실패와 실제 row 부재를 구분하며 PID/start identity가 남거나 lock이 held/unavailable이면 자기치유하지 않는다. |
| 18. prebootstrap registration wait window | `soul-server-ts/src/config.ts:SOUL_RUNNER_LEASE_TIMEOUT_MS`, `runner_process_registry.ts:classifyRunnerRegistration`, `runner_registration_reader.ts:readRunnerRegistrationSummary` | **owner:** local runner recovery classification; **cadence:** 기본 30분 경과 창, 반복 timer 아님; **목적:** kernel lock이 free인 prebootstrap registration의 대기 상한; **판정:** legacy env 이름 유지. DB execution owner lease가 아니다. | active lifecycle은 elapsed time만으로 reap하지 않으며 lock unavailable은 죽음으로 축약하지 않는다. |
| 19. runner lifecycle heartbeat | `soul-server-ts/src/runner/runner_child_runtime_helpers.ts:runnerLivenessIntervalMs`, `runner_child_runtime.ts:startLiveness` (L620–633) | **owner:** active runner child; **cadence:** `min(30초, configured window/3)`, 기본 30초; **목적:** local lifecycle observation 갱신; **판정:** 유지. central execution lease renew·host IPC는 삭제됐고 process liveness owner는 kernel lock이다. | active command·lifecycle·configured interval이 없으면 쓰지 않으며 실패를 로그로 노출한다. |
| 20. initial runner inventory retry | `soul-server-ts/src/upstream/initial_runner_state_sync.ts:sendInitialRunnerState` (L8–58) | **owner:** 새 upstream connection; **cadence:** 100·200·400·800ms, 최대 5회; **목적:** orch에 current runner inventory 전달; **판정:** 유지. | connection 교체·socket close면 즉시 중단한다. |
| 21. exact runner 종료 lock polling | `soul-server-ts/src/runner/runner_process_termination.ts:EXISTING_RUNNER_STOP_TIMEOUT_MS` (L15), `waitForExactProcessExit` (L99–118), `exactProcessIsAbsent` (L120–140) | **owner:** runner termination; **cadence:** SIGTERM/SIGKILL 단계별 최대 2초·25ms kernel-lock 관측; **목적:** held→free 전이로 process death 증명; **판정:** 유지. shell·PID identity probe는 호출하지 않는다. | signal 전 unavailable·다른 owner는 즉시 fail-closed; signal 뒤 release transition의 unavailable만 기한까지 재관측하고 계속 불명확하면 실패한다. |
| 22. legacy writer-lock identity probe | `soul-server-ts/src/runner/runner_process_lock.ts:WINDOWS_PROCESS_PROBE_TIMEOUT_MS` (L31–41), `readProcessStartIdentity` (L224–277), `runner_writer_lock.ts:inspectUnlockedRecord` (L130–154) | **owner:** pre-schema-2 marker migration; **cadence:** kernel endpoint가 free인데 legacy marker가 남은 inspection당 1회, Windows 최대 20초; **목적:** 무중단 롤아웃 중 legacy owner만 3-state로 식별; **판정:** 제거 대기 이행 경로. 정상 schema-2 liveness 예산은 0이다. | `unavailable`을 absent로 축약하지 않고 fail-closed하며, schema-2 marker는 probe 없이 free로 판정한다. |
| 23. terminal session GC gate | `soul-server-ts/src/runner/runner_session_gc_scheduler.ts:SWEEP_INTERVAL_MS` (L5), `schedule` (L21–49), `runner_session_gc.ts:collect` (L43–211) | **owner:** runner recovery scan의 GC scheduler; **cadence:** 유효 inventory가 있을 때 최대 시간당 1회; **목적:** terminal session directory retention 정리; **판정:** 기존 cadence 재사용. retired·kernel-free이고 중앙 row가 없으면 삭제로 폐기된 durable tail을 replay 대상으로 보존하지 않는다. | empty inventory·in-flight·미도래면 skip한다. GC lock 안에서 registration generation과 중앙 부재를 삭제 직전 재검증하며, 중앙 row가 존재·복원되거나 증거가 불명확하면 보존한다. |

이 장을 갱신해야 하는 변경 부류: startup/maintenance cadence·delivery attempt/retry·notification recovery·node-ready/maintenance replay·runtime follow-up watchdog/receipt·event outbox/ingress retry·runner scan/liveness/termination/GC timer 변경.
