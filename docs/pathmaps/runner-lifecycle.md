# 러너 생애주기

최종 대조 커밋 SHA: `d5f21d50833dd530a2688f7778b5980402fee6ee`

> 범위 주석: process liveness의 단일 owner는 OS-enforced kernel writer lock이다. registration id·PID·start identity는 그 lock owner를 식별하고 세대 교체를 fence하는 내구 metadata다. DB execution generation은 event stale fence일 뿐 process owner나 replacement 권한이 아니다.

| 단계 | 파일:심볼(라인) | 이 단계가 소유한 사실 | 거부/분기 조건 |
| --- | --- | --- | --- |
| 1. runtime factory 선택 | `soul-server-ts/src/runner/runner_process_runtime_factory.ts:createRunnerProcessRuntimeFactory` (L92–175) | task·agent·backend·불변 release에서 spawn input과 runner dispatcher를 만든다. | state dir가 없거나 release/MCP/env identity를 구성할 수 없으면 시작을 거부한다. |
| 2. session mutation 직렬화 | `soul-server-ts/src/runner/runner_process_spawn.ts:RunnerProcessSpawner.spawn` (L101–106) | session directory 단위 mutation lock 안에서만 spawn과 기존 registration 정리를 수행한다. | 같은 session mutation lock을 얻지 못하면 spawn 경로에 진입하지 않는다. |
| 3. 기존 owner 정리 | `soul-server-ts/src/runner/runner_process_spawn.ts:spawnLocked` (L108–136), `runner_process_termination.ts:stopExistingRunnerLocked` (L39–80), `runner_writer_lock.ts:prepareRunnerWriterLockForSpawn` (L113–123) | 새 등록 전에 kernel lock의 held/free를 판정해 held owner만 exact 종료하고, free면 PID를 조회하지 않고 schema-2 residue를 회수한다. | lock unavailable·owner metadata mismatch·종료 뒤 lock 미해제면 fail-closed다. |
| 4. pending 등록 | `soul-server-ts/src/runner/runner_process_spawn.ts:spawnLocked` (L133–171), `runner_registration_identity.ts:pendingRunnerRegistrationIdentity` (L34–49) | spawn 전에 UUID registration id와 null PID/start identity를 내구 기록하고 config·SQLite·entry를 준비한다. | session/release identity가 비어 있거나 entry 검증이 실패하면 child를 만들지 않는다. |
| 5. detached spawn | `soul-server-ts/src/runner/runner_process_spawn.ts:spawnLocked` (L172–200) | 불변 snapshot의 `runner_entry.js --config` child를 detached로 만들고 로그 fd를 넘긴다. | child PID가 없거나 spawn/log close가 실패하면 오류다. |
| 6. child identity handshake | `soul-server-ts/src/runner/runner_process_spawn.ts:spawnLocked` (L201–306), `runner_registration_identity.ts:completeRunnerRegistrationIdentityFromChild` (L79–119) | child가 공개한 registration identity 또는 held kernel lock owner를 pending registration과 대조하고 host pid sidecar까지 완성한다. | lock free·unavailable·owner PID mismatch 또는 registration id/start identity mismatch면 exact child만 정리하고 fail-closed다; process probe fallback은 주입된 legacy test adapter에만 남는다. |
| 7A. kernel writer lock 획득 | `soul-server-ts/src/runner/runner_child_runtime.ts:RunnerChildRuntime.start` (L91–103), `runner_writer_lock.ts:RunnerWriterLock.acquire` (L41–64), `runner_kernel_lock.ts:RunnerKernelLock.tryAcquire` (L21–42) | child가 Linux abstract socket 또는 Windows named pipe를 배타 bind해 process-lifetime liveness owner가 되고 schema-2 owner metadata를 기록한다. | endpoint가 이미 bind됐거나 residue 판정이 unavailable이면 child startup을 거부한다. |
| 7B. legacy marker 이행 | `soul-server-ts/src/runner/runner_writer_lock.ts:inspectRunnerWriterLock` (L87–111), `inspectUnlockedRecord` (L130–154), `runner_process_lock.ts:readProcessStartIdentity` (L224–277) | kernel endpoint가 free인데 pre-schema-2 marker가 남은 롤아웃 표본만 3-state start-identity probe로 판정한다. | `unavailable`은 부재로 축약하지 않고 fail-closed하며, schema-2 정상 경로는 이 probe를 호출하지 않는다. |
| 8. child runtime 가동 | `soul-server-ts/src/runner/runner_child_runtime.ts:RunnerChildRuntime.start` (L104–143) | SQLite outbox·lifecycle·host socket·engine dispatcher를 한 child runtime에 연다. | 각 저장/소켓 경계 실패는 child 시작 실패로 귀결된다. |
| 9. lifecycle heartbeat | `soul-server-ts/src/runner/runner_child_runtime.ts:recordLiveness` (L614–618), `startLiveness` (L620–633) | active command의 local lifecycle `progressed_at`만 같은 cadence로 갱신한다. central execution lease renew IPC·host call은 없고 process liveness는 kernel lock이 소유한다. | active command·lifecycle·configured interval이 없으면 기록하지 않는다. |
| 10. recovery inventory | `soul-server-ts/src/runner/runner_recovery_coordinator.ts:performScan` (L146–298), `runner_process_registry.ts:scanRunnerRegistrations`, `runner_registration_reader.ts:readRunnerRegistrationSummary` | node-local registration 전량을 읽고 kernel lock에서 보수적 생사 값을 산출해 session별 처리 후보를 만든다. lifecycle의 `runner_pid`는 과거 관측값으로 보존하며 process liveness를 소유하지 않는다. 중앙 owner-null inventory·backfill·ownership backoff는 없다. | lock unavailable은 죽음으로 축약하지 않으며 이미 active인 session과 adoption failure 대기는 중복 처리하지 않는다. |
| 11. disposition 결정 | `soul-server-ts/src/runner/runner_process_registry.ts:classifyRunnerRegistration` (L133–168) | lifecycle·retired flag·kernel-lock 기반 생사 값을 wait/adopt/replay/reap/closed로 완전 분류한다. | `progress_at` 경과만으로 죽음을 선언하지 않는다; lock unavailable은 보수적으로 live 취급하고 adopt에서 exact owner를 다시 증명한다. |
| 12. exact adopt | `soul-server-ts/src/runner/runner_process_spawn.ts:RunnerProcessSpawner.adopt` (L308–338), `runner_process_runtime_factory.ts:adoptRegisteredRunner` (L229–238) | registration id·PID·start identity와 held kernel lock owner가 모두 같은 child만 adopted handle로 만든다. | 필드 누락·retired·lock free/unavailable·owner mismatch면 null/오류다. |
| 13. task 재부착 | `soul-server-ts/src/runner/runner_recovery_coordinator.ts:recoverRegistered` (L451–606), `runner_recovery_task.ts:prepareRecoveredTask` (L58–64), `task/task_executor.ts:recoverRegisteredRunner` (L675–728) | agent snapshot과 backend session id를 task에 복원하고 adopt/live replay/offline replay mode로 durable event stream을 다시 소비한다. 별도 `recoveredExecutionOwnership` 복원이나 DB owner 판정은 없다. | 기존 runner/execution이 새 주체를 막으면 겹치지 않으며 terminal offline replay 차단은 경고한다. |
| 14. exact terminate | `soul-server-ts/src/runner/runner_registration_control.ts:RunnerRegistrationControl.terminate` (L31–46), `runner_process_spawn.ts:RunnerProcessSpawner.terminate` (L340–366), `runner_process_termination.ts:stopExistingRunnerLocked` (L39–80) | registration metadata가 기대 owner를 fence하고 kernel lock이 실제 생존과 signal 대상을 결정한 뒤 한 mutation lock에서 종료·invalidation한다. | terminal release 증거 없이 expected가 없거나 lock owner가 기대 metadata와 다르면 거부한다. |
| 15. exact process 종료 증명 | `soul-server-ts/src/runner/runner_process_termination.ts:terminateExactRunner` (L82–97), `waitForExactProcessExit` (L99–118), `exactProcessIsAbsent` (L120–140) | SIGTERM→SIGKILL 뒤 kernel lock의 held→free 전이로 원래 runner 사망을 증명하며 PID를 재조회하지 않는다. | signal 전 unavailable·다른 owner는 즉시 fail-closed; signal 뒤 release 중 transient unavailable만 기존 2초 기한까지 재관측하고 계속 불명확하면 실패한다. |
| 16. incomplete identity dispose | `soul-server-ts/src/runner/runner_registration_control.ts:RunnerRegistrationControl.terminate` (L20–45), `runner_process_spawn.ts:disposeUnprovenRegistration` (L368–381), `runner_process_termination.ts:stopExistingRunnerLocked` (L39–80) | PID/start identity가 불완전해도 held lock owner는 exact 종료하고, free lock이면 stale PID를 조회·signal하지 않은 채 residue만 격리한다. | dispose capability가 없거나 lock unavailable이면 spawn·kill을 모두 거부한다. |
| 17. registration invalidate | `soul-server-ts/src/runner/runner_process_spawn.ts:invalidateRegistration` (L383–388), `runner_registration_control.ts:invalidate` (L48–53) | 기대 registration id에 맞는 sidecar·socket·pid 증거를 폐기한다. | registration id가 바뀌었으면 다른 generation의 증거를 지우지 않는다. |
| 18. terminal retire | `soul-server-ts/src/runner/runner_registration_control.ts:retireTerminal` (L49–57), `runner_process_spawn.ts:retireTerminalRegistration`, `runner_terminal_identity_retirement.ts:retireReleasedTerminalExecutionEvidence`, `runner_recovery_coordinator.ts:handle` (L310–448), `task/task_lifecycle_route.ts:deleteTask` | canonical terminal이 이미 기록된 registration은 DB owner release/reproof 없이 local exact registration evidence를 retire한다. 중앙 row가 먼저 사라진 과거 residue는 recovery가 중앙 부재와 kernel-lock free를 함께 증명한 뒤 released-terminal retirement를 재사용한다. | registration identity가 바뀌었거나 lifecycle이 non-terminal인 채 PID가 남거나 lock이 held/unavailable이면 retirement를 거부한다. 중앙 row 부재 재검사는 삭제 residue에만 남는다. |
| 19. shutdown lock 해제 | `soul-server-ts/src/runner/runner_child_runtime.ts:RunnerChildRuntime.shutdown` (L157–171), `runner_writer_lock.ts:RunnerWriterLock.release` | engine→socket→local lifecycle timer→lifecycle/outbox→schema-2 metadata→kernel endpoint 순서로 child 자원을 닫는다. | 중복 shutdown은 no-op; metadata owner가 바뀌면 오류다. metadata unlink와 endpoint close 사이 transient unavailable은 종료 신호 뒤 polling에서만 재관측한다. |

이 장을 갱신해야 하는 변경 부류: runner release/spawn·registration identity·kernel writer lock·lifecycle heartbeat·execution generation record·recovery classification/adoption·termination/dispose·terminal retirement/GC 변경.
