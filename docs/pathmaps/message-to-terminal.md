# 메시지 도착에서 종단까지

최종 대조 커밋 SHA: `c855db5d7e5afaefc86ce8ee1e80cd996d8d2478`

> 상태 배지: **R32 · R33 · R34-③ · R35 코드 반영**

> 범위 주석: REST·내부 세션 송신·노드 명령은 `TaskManager.addIntervention` 이후 하나의 경로로 수렴한다.

| 단계 | 파일:심볼(라인) | 이 단계가 소유한 사실 | 거부/분기 조건 |
| --- | --- | --- | --- |
| 1. 공개 메시지 접수 | `orch-server-ts/src/session/session_action_command_routes.ts:registerSessionActionCommandRoutes` (L61–103) | `POST /api/sessions/:session_id/intervene`의 본문·대상 세션·caller를 wire payload로 확정한다. | JSON 객체나 `intervenePayload` 계약이 아니면 400이다. |
| 2. 공개 메시지 내구 등록 | `orch-server-ts/src/session/session_action_command_routes.ts:admitDurableHumanIntervention` (L207–265) | `delivery_id`·`completion_id`·`relation_key`·canonical payload를 만들고 `session_deliveries`에 최초 등록한다. | repository가 없거나 다른 intent면 통과시킨다; identity 충돌이면 409다. |
| 3. 소유 노드 라우팅 | `orch-server-ts/src/session/session_action_command_errors.ts:sendInterveneCommand` (L47–90), `dispatchActionCommand` (L215–225) | 세션 소유 노드와 현재 연결을 골라 intervene 명령을 전송한다. | 소유 노드 없음·stale·연결 없음은 route 오류; 내구 등록 뒤 불명확한 전송 실패는 queued ACK로 수렴한다. |
| 4. 노드 명령 수신 | `soul-server-ts/src/upstream/intervention_command_family.ts:handleIntervene` (L115–153) | cross-node 명령을 `TaskRuntimeCommands.intervene` 호출과 request-id ACK로 변환한다. | 세션 ID·본문 누락은 거부; request id가 없으면 ACK만 생략한다. |
| 5. 내부 송신 진입 | `soul-server-ts/src/task/session_message_sender.ts:sendMessageToSession` (L71–132) | 에이전트/MCP 내부 송신을 소유 노드에 따라 local `addIntervention` 또는 orch relay로 보낸다. | 대상 소유권을 못 찾거나 relay 응답이 retryable 실패면 명시적 오류다. |
| 6. 노드 명령의 로컬 변환 | `soul-server-ts/src/upstream/task_runtime_commands.ts:TaskRuntimeCommands.intervene` (L214–235) | wire의 identity·caller·첨부·context를 `TaskManager.addIntervention` 인자로 보존한다. | 여기서는 전달 정책을 결정하지 않는다; 하위 경로의 결과를 그대로 반환한다. |
| 7. 단일 메시지 경계 | `soul-server-ts/src/task/task_manager.ts:TaskManager.addIntervention` (L487–492), `task_intervention_route.ts:TaskInterventionRoute.addIntervention` (L118–163) | 모든 세션 지향 메시지를 한 ownership·delivery 경계로 모으고 저장 원장 payload를 복원한다. | identity 불일치·이미 consumed/dead-letter·동시 claim이면 suppressed다. |
| 8. 전달 claim | `soul-server-ts/src/task/task_delivery_ledger_gate.ts:TaskDeliveryLedgerGate.admit` (L56–142) | `pending/queued → claimed`를 대상 세션·lease owner에 CAS하고 이번 시도의 전달 소유자를 정한다. | v2 비대상은 legacy; 필수 identity 누락은 오류; terminal aggregate는 재진입하지 않는다. |
| 9. dispatch 시작 | `soul-server-ts/src/task/task_intervention_route.ts:TaskInterventionRoute.addIntervention` (L188–202), `task_delivery_ledger_gate.ts:beginDispatch` (L145–164) | task 초기화가 끝난 뒤 `claimed → dispatching`으로 전진시켜 routing 판정과 ledger 시도를 결합한다. | lease 소유자·만료·producer terminal revision fence가 맞지 않으면 suppressed다. |
| 10. 실행 경로 분류 | `soul-server-ts/src/task/task_intervention_route.ts:interventionTaskRoute` (L378–390) | non-active는 `auto-resume`, active+live execution evidence만 `running`, initializing은 `activating`으로 분류한다. | `executionPromise`만으로 live로 보지 않는다; initializing이 남아 있으면 오류다. |
| 11. 실행 중 전달 | `soul-server-ts/src/task/task_intervention_route.ts:TaskInterventionRoute.addIntervention` (L208–230), `task_running_intervention_transition.ts:RunningInterventionTransition.deliver` (L53–77), `tryIntervene` (L162–197), `runner/runner_process_engine_proxy.ts:intervene` (L45–55), `runner/runner_ipc_connection.ts:RunnerIpcRequestTimeoutError` (L42–47), `RunnerIpcConnection.request` (L115–153) | live 실행에는 즉시 deliver 또는 durable queue를 선택하고, 과거 dispatch 이력이 있는 human retry는 queue-only로 고정한다. running delivery는 engine 호출보다 먼저 `interruptRequest` barrier를 설치하므로, 현재 턴의 terminal 꼬리에서 queue가 완성되는 경우도 14단계 driver가 그 barrier를 기다릴 수 있다. process runner의 intervention이 기존 30초 IPC deadline을 넘으면 lifecycle `interrupt`가 같은 runner 안의 막힌 Query를 닫아 다음 주입이 새 Query로 이어지게 한다. | timeout 전에는 산 턴을 건드리지 않는다. IPC timeout 외 engine 오류는 기존 `verdict_unknown` 보존 경로를 유지하며, 회복은 runner 프로세스 종료나 세션 error 전이를 만들지 않는다. |
| 12. auto-resume 진입 fence | `soul-server-ts/src/task/task_auto_resume_transition.ts:AutoResumeTransition.resume` (L65–91) | 기존 실행 drain·stale engine close·caller 승격 전에 상태를 `initializing`과 activation으로 고정한다. | `running`이면서 live evidence가 있거나 resumable profile이 없으면 거부한다. |
| 13. running 전이와 메시지 기록 | `soul-server-ts/src/task/task_auto_resume_transition.ts:AutoResumeTransition.resume` (L93–140) | user message와 terminal revision fence를 가진 canonical running transition을 기록하고 메모리 투영을 적용한다. | transition CAS가 거부되면 원래 status·terminal id로 되돌리고 activation을 reject한다. |
| 14. durable queue 선행·terminal 잔여 구동 | `soul-server-ts/src/task/task_intervention_route.ts:TaskInterventionRoute.addIntervention` (L254–290), `task_delivery_ledger_gate.ts:recordResult` (L203–287), `task_executor.ts:holdExecutionSlot` (L446–455), `driveQueuedAfterTerminal` (L457–482), `task_manager.ts:resumeQueuedAfterTerminal` (L295–300), `task_auto_resume_transition.ts:resumeQueuedAfterTerminal` (L54–63) | 일반 auto-resume은 결과를 `queued/pending`에 먼저 저장한 뒤 deferred `onResume`을 단 한 번 호출한다. running delivery가 terminal 꼬리에 queued로 남으면 execution slot 해제 driver가 기존 intervention barrier를 기다리고 terminal+queue를 재검사한 뒤 같은 `AutoResumeTransition` owner를 즉시 호출한다. 이미 발행한 user message는 재발행하지 않는다. | 새 timer·scheduler·lease·ledger owner는 없다. 새 실행이 이미 생겼거나 task가 terminal이 아니거나 queue가 비었으면 no-op이며, queue CAS·notification outbox stage 실패 때는 기존 경로처럼 스폰하지 않는다. |
| 15. 새 실행 시작 | `soul-server-ts/src/task/task_executor.ts:TaskExecutor.startNewExecution` (L267–299), `startExecutionWithoutOwnership` (L408–426) | backend·runner를 선택하고 task execution slot에 하나의 실행 promise를 건다. | 기존 runner나 다른 activation이 있으면 동시 실행을 거부한다. |
| 16. 러너 스폰 | `soul-server-ts/src/runner/runner_process_runtime_factory.ts:createRunnerProcessRuntimeFactory` (L134–175), `runner_process_spawn.ts:RunnerProcessSpawner.spawn` (L101–306) | 불변 release·session config로 replacement 경계를 열고 kernel lock이 free임을 증명한 뒤 pending registration과 detached child를 만든다. | 기존 lock owner 정리·lock free 증명·entry 검증·child lock handshake 중 하나라도 실패하면 fail-closed다. |
| 17. 자식 identity와 kernel lock | `soul-server-ts/src/runner/runner_child_runtime.ts:RunnerChildRuntime.start` (L91–143), `runner_writer_lock.ts:RunnerWriterLock.acquire` (L41–64), `runner_kernel_lock.ts:RunnerKernelLock.tryAcquire` (L21–42), `runner_registration_identity.ts:completeRunnerRegistrationIdentityFromChild` (L79–119) | 자식이 OS-enforced endpoint를 배타 bind해 process-lifetime liveness owner가 되고, 그 owner PID·start identity를 registration에 완성한 뒤 socket을 연다. | endpoint가 이미 held·unavailable이거나 pending registration이 다른 session/release/process를 가리키면 시작을 거부한다. |
| 18. 세션 준비와 실행 활성화 | `soul-server-ts/src/task/task_executor.ts:TaskExecutor.startExecutionWithRunner` (L629–674) | runner session을 준비하고 durable intervention을 복원한 뒤 status를 `running`으로 바꾸고 activation을 resolve한다. | session 준비·복원·stream 소비 실패는 outer failure finalizer로 간다. |
| 19. 프롬프트 주입 | `soul-server-ts/src/task/task_turn_input_builder.ts:prepareInitialTurnInput` (L57–67), `prepareFollowupTurnInput` (L69–100), `context/context_builder.ts:ExecutionContextBuilder.buildFollowupContext` (L128–168) | 초기/후속 prompt와 caller·thread·running-session context를 현재 턴 입력으로 조립한다. | intervention queue가 있으면 initial 입력도 follow-up 조립 경로를 사용한다. |
| 20. 엔진 턴 | `soul-server-ts/src/task/task_engine_turn_runner.ts:TaskEngineTurnRunner.executeTurn` (L83–162), `task_executor.ts:consumeTurnLoop` (L910–985), `engine/claude_session_runtime.ts:enqueueInput` (L161–177) | backend resume id·input uuid·intervention ids를 프레임에 싣고 SSE 이벤트를 순서대로 소비한다. Claude persistent runtime은 UUID를 수신 정본으로 삼아 이미 본 UUID의 재입력을 payload hash와 무관하게 skip하고 기존 Query를 살린다. | 새 UUID만 input queue에 들어간다. compact/rollover/follow-up 상태에 따라 다음 턴 입력을 재조립한다. |
| 21. delivery 소비 증거 | `soul-server-ts/src/task/task_executor.ts:observeDeliveryTurn` (L1162–1168), `settleDeliveryTurn` (L1170–1182), `handleClaudeRuntimeFollowupReceipt` (L1466–1519), `consumeTerminalTurnReceipts` (L1564–1569), `task_delivery_turn_receipt.ts:hasConsumptionReceipt` (L52–56) | 턴 이벤트를 receipt에 관찰시키고 성공한 continue/terminal 턴에서 exact delivery를 consumed로 확정한다. runtime follow-up은 receipt가 있으면 빈 응답도 소비된 무행동으로 종결하고, receipt가 없으면 같은 delivery·stable input UUID를 실행 로컬 FIFO에서 한 번만 다시 전달한다. | exact replay가 대기 중일 때만 `task_intervention_queue.ts:dequeueInterventionsInLane` (L67–78)로 high-priority 사용자 turn을 먼저 별도 실행한다. replay가 없으면 기존 high+low 단일 turn을 유지한다. 두 번째도 receipt가 없으면 원 delivery를 미소비로 남겨 startup transcript recovery가 재생할 수 있게 하며 child delivery·timer·attempt wrapper·fatal error·세션 중단은 만들지 않는다. receipt 전 dispatch 고착은 11단계의 기존 IPC deadline 회복으로 유한화한다. |
| 22. terminal 진입 | `soul-server-ts/src/task/task_executor.ts:_consumeEventStream` (L884–907), `task_executor_finalizer.ts:TaskExecutorFinalizer.finalize` (L19–37) | stream 종료 뒤 terminal persist → engine close → 성공 delivery consume → completion notify 순서를 소유한다. | terminal transition이 적용되지 않으면 delivery consume·completion notify를 실행하지 않는다. |
| 23. terminal 정본 기록 | `soul-server-ts/src/task/task_lifecycle_transition.ts:TaskLifecycleTransition.persistFinalState` (L312–350), `enqueueAndAwaitSessionEnded` (L397–461), `packages/db-schema/sql/schema.sql:session_apply_terminal_transition` (L1597–1660), `session_project_recovered_runner_terminal_fact` (L3186–3309) | `session_ended` 이벤트와 ownership release/recovered fact/일반 terminal effect 중 정확히 하나를 enqueue하고 canonical session을 적용한다. recovered fact는 session row lock 안에서 exact active owner를 해제하고, exact owner가 없으며 살아 있는 경쟁 owner도 없으면 같은 트랜잭션의 ownerless terminal CAS로 수렴한다. | 이미 기록된 recovered terminal은 reconcile 경로다. 다른 active owner나 현재 시각 기준 미만료 reservation·identity proof, sessions 현재 owner 필드, 더 최신 terminal event 중 하나라도 있으면 거부한다. 만료된 owner 이력과 역사적 generation은 terminal을 막지 않는다. |
| 24. orch 투영과 방송 | `orch-server-ts/src/node/event_ingress_controller.ts:NodeEventIngressController.process` (L71–184), `committedEffectSessionUpdate` (L194–237) | DB commit 결과의 canonical session effect를 cache에 먼저 적용하고 event·`session_updated`를 방송한 뒤 ACK한다. | invalid batch는 연결을 닫는다; duplicate receipt는 canonical projection 유무에 따라 update를 생략한다. |
| 25. terminal 의미 fence | `orch-server-ts/src/session/session_event_semantic_publication.ts:shouldPublishSessionEventSemantically` (L6–12) | session effect가 실제 적용된 `session_ended`만 의미 이벤트로 공개한다. | terminal CAS가 거부된 `session_ended`는 방송하지 않는다. |
| 26. 호출자 통지 | `soul-server-ts/src/task/task_executor_finalizer.ts:TaskExecutorFinalizer.notifyCompletion` (L104–117), `completion_notifier.ts:TaskCompletionNotifier.notify` (L105–149) | canonical terminal receipt가 있는 새 완료만 caller delivery로 등록한다. | caller 없음·self caller·`notifyCompletion=false`·terminal receipt 없음이면 통지하지 않는다. |

이 장을 갱신해야 하는 변경 부류: intervene 진입·delivery admission/route·auto-resume·runner spawn·context 주입·turn loop·runtime follow-up receipt·terminal persistence/projection·completion notification 변경.
