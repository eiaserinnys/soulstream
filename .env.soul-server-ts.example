# Soulstream TypeScript worker configuration

# Required
SOULSTREAM_NODE_ID=standalone-ts
# Set to a TS node ID for legacy node hosting, or `orch` when the orchestrator hosts Y.Doc.
# `orch` is reserved and cannot be used as SOULSTREAM_NODE_ID.
BOARD_YJS_HOST_NODE_ID=standalone-ts
SOULSTREAM_UPSTREAM_URL=ws://127.0.0.1:5200/ws/node
DATABASE_URL=postgresql://user:password@127.0.0.1:5432/soulstream_test

# Local HTTP server
HOST=127.0.0.1
PORT=3105
ENVIRONMENT=development
LOG_LEVEL=info

# Auth
# Required when ENVIRONMENT=production.
AUTH_BEARER_TOKEN=

# MCP post-start health contract
MCP_ENABLED=true
MCP_PATH=/mcp
MCP_REQUIRE_AUTH=false

# Agent registry
AGENTS_CONFIG_PATH=config/agents.yaml
MODEL_CATALOG_PATH=config/model-catalog.yaml
TURN_SUMMARY_CONFIG_PATH=config/turn-summary.yaml
# Optional direct API provider credential. Never rename to OPENAI_API_KEY.
# TURN_SUMMARY_OPENAI_KEY=

# Runtime files
INCOMING_FILE_DIR=.local/incoming

# Claude persistent runtime v2 (default-off; do not enable before canary approval)
CLAUDE_SESSION_RUNTIME_V2_ENABLED=false
CLAUDE_SESSION_RUNTIME_IDLE_TTL_MS=300000
CLAUDE_SESSION_RUNTIME_MAX_ENTRIES=16
CLAUDE_SESSION_RUNTIME_TURN_TIMEOUT_MS=1800000

# Optional user display metadata advertised to the orchestrator
DASH_USER_NAME=
DASH_USER_PORTRAIT=

# Supervisor
SUPERVISOR_ENABLED=false
SUPERVISOR_EVENT_INGEST_ENABLED=false
